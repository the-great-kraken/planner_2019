package com.example.planner_.screens.lists;

import androidx.lifecycle.LiveData;
import androidx.lifecycle.ViewModel;

import com.example.planner_.App;
import com.example.planner_.model.ListModel;

import java.util.List;

public class ListViewModule extends ViewModel {
    //private LiveData<List<ListModel>> listLiveData = App.getInstance().getListDao().getAllLiveData();

    //public LiveData<List<ListModel>> getNoteLiveData() {
        //return listLiveData;
 //   }
}
